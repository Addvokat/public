def get_even_numbers_up_to(number):
    counter = 1
    result = ''
    while counter <= number:
        if counter % 2 == 0:
            result = result + str(counter) + ','
        counter += 1
    new_result = result[0:-1] + '.'

    
    return new_result


print(get_even_numbers_up_to(18))
